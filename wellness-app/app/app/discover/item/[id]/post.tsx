import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function DiscoverItemPostScreen() {
  return (
    <ScreenPlaceholder
      title="口コミ投稿"
      screenNo={12}
      note="星・目的・使用期間・継続中・購入先・本文（20文字以上）を入力する"
    />
  );
}
