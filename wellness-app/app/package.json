{
  "name": "@wellness/app",
  "version": "0.1.0",
  "private": true,
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "ios": "expo run:ios",
    "typecheck": "tsc --noEmit",
    "doctor": "npx expo-doctor"
  },
  "//": "依存はあえて空。bash scripts/bootstrap-app.sh で `expo install` に解決させる（SDK整合を手で書かない）",
  "dependencies": {},
  "devDependencies": {}
}
